'use client';

import type { LinkProps } from '@mui/material/Link';

export type LogoProps = LinkProps & { isSingle?: boolean; disabled?: boolean };
export function Logo(_props: LogoProps) { return null; }
